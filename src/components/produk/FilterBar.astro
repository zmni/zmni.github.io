---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"menu">;

const { ...attrs } = Astro.props;
---

<menu
  {...attrs}
  class:list={["mb-4 flex flex-col gap-4 md:flex-row md:justify-center lg:mb-8"]}
  data-role="produk-filter"><slot /></menu
>
