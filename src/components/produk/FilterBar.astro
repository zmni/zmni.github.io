---
type Props = {
  class?: string;
};

const { class: className } = Astro.props;
---

<menu class:list={["mb-4 gap-4 flex flex-wrap sm:justify-center lg:mb-8", className]} data-role="produk-filter">
  <slot />
</menu>
